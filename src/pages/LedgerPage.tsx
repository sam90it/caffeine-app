import React from "react";import { useGetSummaryDashboard, useGetAllPeople } from "../hooks/useQueries";export default function LedgerPage({ onCreateEntry }: any) { const { data: summary, isLoading: summaryLoading } = useGetSummaryDashboard(); const { data: people, isLoading: peopleLoading } = useGetAllPeople(); if (summaryLoading || peopleLoading) return <div className="p-8 text-slate-400 font-medium">Synchronizing Ledger...</div>; return ( <div className="space-y-8 animate-in fade-in duration-500"> <div className="grid grid-cols-1 md:grid-cols-3 gap-6"> {[ { label: "Total Lent", value: summary?.totalLent, color: "text-blue-600" }, { label: "Total Repaid", value: summary?.totalRepaid, color: "text-emerald-600" }, { label: "Remaining Due", value: summary?.remainingDue, color: "text-slate-900" } ].map((item, i) => ( <div key={i} className="bg-white p-6 rounded-xl border border-slate-200 shadow-sm"> <p className="text-xs font-semibold text-slate-500 uppercase tracking-wider">{item.label}</p> <p className={`text-2xl font-bold mt-2 ${item.color}`}> {Number(item.value || 0).toLocaleString()} <span className="text-sm font-normal text-slate-400">INR</span> </p> </div> ))} </div> <div className="bg-white rounded-xl border border-slate-200 shadow-sm overflow-hidden"> <div className="p-6 border-b border-slate-100 flex justify-between items-center"> <h3 className="font-bold text-slate-800">Global Ledger Accounts</h3> <button onClick={onCreateEntry} className="text-sm bg-slate-900 text-white px-4 py-2 rounded-lg hover:bg-slate-800 transition-all" > New Transaction </button> </div> <table className="w-full text-left border-collapse"> <thead> <tr className="bg-slate-50 text-slate-500 text-xs uppercase font-bold"> <th className="p-4 border-b">Entity</th> <th className="p-4 border-b">Verification</th> <th className="p-4 border-b text-right">Net Position</th> </tr> </thead> <tbody> {people?.map((person: any) => ( <tr key={person.id.toString()} className="hover:bg-slate-50 transition-colors border-b border-slate-50"> <td className="p-4 font-medium text-slate-700">{person.name}</td> <td className="p-4"> <span className={`px-2 py-1 rounded-full text-[10px] font-bold uppercase ${person.approvalStatus ? "bg-emerald-100 text-emerald-700" : "bg-slate-100 text-slate-600"}`}> {person.approvalStatus ? "Verified" : "Pending"} </span> </td> <td className="p-4 text-right font-mono font-bold text-slate-900">--</td> </tr> ))} </tbody> </table> </div> </div> ); }